<?xml version="1.0" encoding="utf-8"?>
<!--
  PoC network policy: cleartext HTTP is permitted ONLY for the Docker
  Compose stack on the host (10.0.2.2 from emulator). All other domains
  default to HTTPS-only enforcement.

  Production deployments will replace this entirely with HTTPS-everywhere.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
